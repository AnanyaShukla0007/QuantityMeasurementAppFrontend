export const environment = {
  production: false,
  // ⚠️  Match the port your backend actually runs on.
  //     Your backend .http file shows localhost:5027.
  //     Run the API project and check the terminal for the actual port.
  apiUrl: 'https://localhost:5001/api'
};
